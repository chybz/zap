#include <iostream>

#include <zap/cmdline.hpp>
#include <zap/utils.hpp>
#include <zap/env.hpp>

int main(int ac, char** av)
{
    zap::env e;

    // int rc = 0;

    // auto cl = zap::parse(ac, av);

    // if (cl.exit) {
    //     rc = 1;
    // } else {
    //     cl.run();
    // }
    //
    // return rc;

    return 0;
}
